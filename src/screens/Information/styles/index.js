import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  mainContainer: {
    flex: 1,
    marginLeft: 10,
    paddingLeft: 20,
    paddingTop: 50, // cambiar valor a 20
  },
});